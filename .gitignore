.env
.pi/

# Rendered secrets (gitignored, never committed)
stacks/*/.env
stacks/**/*.env

# Age key and deploy key (sensitive)
secrets/age.key
secrets/pkunited_deploy_ed25519
secrets/pkunited_deploy_ed25519.pub

# Node modules (if any tooling added later)
node_modules/

# Python venvs
.venv/
*.pyc
__pycache__/
